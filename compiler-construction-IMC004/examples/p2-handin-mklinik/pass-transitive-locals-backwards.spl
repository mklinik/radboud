// The return type of the function constrains all local variables to be of type $\text{Int}$, which
// constrains the function argument to be also of type $\text{Int}$.

Int f(var a)
{
  var z = a;
  var y = z;
  var x = y;
  return x;
}
